[build-system]
requires = ["setuptools>=5"] # , "setuptools_scm[toml]>=7.1"]
build-backend = "setuptools.build_meta"

[project]
name = "apikeymanager"
description = "Proxy service that dynamicaly transform CSV, XML, JSON into valid GeoJson"
readme = "README.md"
keywords = ["apikeymanager", "api-key"]
license = {text = "Apache-2.0"}
classifiers = [
    "Programming Language :: Python :: 3",
]
dependencies = [
    "uvicorn[standard]",
    "fastapi",
    "xmltodict",
    "python-jose",
    "SQLAlchemy",
    "psycopg2_binary",
    "pydantic_settings",
    "aiohttp",
    "slowapi",
    "httpx",
    "python-keycloak",
]
dynamic = ["version"]

[project.optional-dependencies]
dev = [
    "black",
    "flake8",
    "flake8-quotes",
    "flake8-annotations",
    "flake8-bandit",
    "flake8-bugbear",
    "isort",
    "mypy",
    "pre-commit",
    "pytest",
    "pytest-cov",
    "pytest-html",
    "pytest-mock",
    "requests",
    "twine",
    "pytest-docker"
]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
    "integration",
]
#log_cli = 1
#log_cli_level=INFO
#log_format = %(asctime)s %(levelname)s %(message)s
#log_date_format = %Y-%m-%d %H:%M:%S

[tool.setuptools]
py-modules = ["app"]

[tool.setuptools_scm]
write_to = "app/_version.py"
write_to_template = '''# This file is part of APIKeyManger project
#     https://gitlab.si.c-s.fr/space_applications/apikeymanager/
# File generated by setuptools_scm
# don't change, don't track in version control
__version__ = "{version}"
'''

[tool.black]
line-length = 88

[tool.pycln]
all = true

[tool.isort]
line_length = 88
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
profile = "black"

[tool.check-manifest]
ignore = [".vscode/*", "*.faas", "Dockerfile"]
